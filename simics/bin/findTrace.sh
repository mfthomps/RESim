#!/bin/bash
#
#  Use grep to find content of syscall traces
#  generated by afl queue files.  Example:
#  findTrace.sh open "| grep -v time"
#
here=$(pwd)
base=$(basename $here)
afldir=$AFL_DATA/output/$base
searchdir=$afldir/*/trace/*
cmd="grep $1 $searchdir $2 | less"
eval $cmd
